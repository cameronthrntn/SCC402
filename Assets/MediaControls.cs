using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class MediaControls : MonoBehaviour {
	
	/* private AudioSource assistantAudioSource;
	private Camera camera;
	private GameObject AssitantContainer;
	
	

	void Start() {
		this.setActive(false)
		camera = GetComponent<Camera>();
        assistantAudioSource = assistant.GetComponent<AudioSource>();
		AssitantContainer = GetComponent<AssitantContainer>();
		
	}
	
	
	void Update(){
		
		if (assistantAudioSource.isPlaying) {
		  this.transform.position = AssitantContainer.transform.position
		  
		  this.setActive(true);
		}
		
	} */
}